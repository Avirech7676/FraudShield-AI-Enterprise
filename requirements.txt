# ==========================================================================
# FraudShield AI Enterprise - backend requirements (CORRECTED)
# All versions below are REAL, installable releases on PyPI.
# The previous file listed invented versions (fastapi==0.139.0, numpy==2.4.6,
# pandas==3.0.3, scikit-learn==1.9.0, ...) that do not exist -> pip install failed.
#
# IMPORTANT: scikit-learn / xgboost / lightgbm / catboost must match the versions
# you TRAINED your .joblib models with, or joblib.load(...) may fail.
# Check your training machine with:  pip show scikit-learn xgboost lightgbm catboost
# and adjust the pins below to match.
# ==========================================================================

# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
pydantic==2.10.4
python-multipart==0.0.20

# --- Data / ML ---
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.5.2
imbalanced-learn==0.12.4
xgboost==2.1.3
lightgbm==4.5.0
catboost==1.2.7
shap==0.46.0
joblib==1.4.2

# --- Streaming ---
kafka-python==2.0.2
pika==1.3.1  # For RabbitMQ support

# --- Plotting (used by dashboard / reports) ---
matplotlib==3.9.3
seaborn==0.13.2
plotly==5.24.1

# --- Database ---
pymongo==4.10.1
motor==3.7.1

# --- Auth / security ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
passlib==1.7.4

# --- AI / LLM ---
groq==0.13.1

# --- Exports / misc ---
reportlab==4.2.5
python-docx==1.1.2
openpyxl==3.1.5
Pillow==11.0.0
psutil==6.1.0
requests==2.32.3
python-dotenv==1.0.1